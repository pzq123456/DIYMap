# DIYMap
DIY A MAP

``` bash
src/
│
├── main.ts                    // 入口文件，应用初始化
├── map.ts                     // 地图初始化与样式控制
├── style.ts                   // 页面样式控制（背景色、字体颜色）
├── print-ui.ts                // 打印设置 UI 和逻辑
├── search.ts                  // 城市搜索功能
└── constants.ts               // 常量定义（如样式文件列表、默认值等）
```
